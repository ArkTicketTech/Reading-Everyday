代理模式继续。
代理模式的定义：代理模式为另一个对象提供一个替身或占位符以控制对这个对象的访问。
使用代理模式创建代表对象，让代表对象控制某对象的访问，被代理的对象可以是远程的对象，创建开销大的对象或需要安全控制的对象。
远程代理我们已经熟悉了，那么虚拟代理是什么呢。
虚拟代理也很简单，就是在开销完成之前的一个替代品。
比如这里cd封面还未加载完成时，使用的一行文字。
这个其实通过回调的方式可以很好地解决，看看书里面是怎么写的。
