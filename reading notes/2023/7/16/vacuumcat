来看单例模式。
对于类似于kotlin这样比较新的语言而言，单例模式只需要一个object关键字即可使用。
但仍然可以看看Java中是如何实现的。
也很简单，使用私有的构造函数即可。
然后公开一个getInstance方法，在其中用一个静态变量判断当前是否存在实例。
然而，这样会导致多线程同步的问题。
解决多线程同步的问题，可以使用synchronized注解，但这样很显然会导致一定的性能问题。
如何解决这点，等接下来书里面的内容解答。
