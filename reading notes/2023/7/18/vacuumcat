继续来看设计模式。
还是多线程的问题。
首先，如果程序可以接受同步带来的负担，那么就什么都不要做。
或者，也可以在静态初始化器中创建单件。
或者进行优化，只有在实例尚未创建时才进行同步操作。
这样可以大大提高性能。
多个类加载器可能会导致问题，需要注意。
而使用类的单件可能会导致与初始化次序有关的问题，也是不建议使用的。
这里提到一点，全局变量在某些情况下可能有多个实例！因此单例模式还是最好的办法。
