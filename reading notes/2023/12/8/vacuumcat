虽然还在读凤凰架构，但今天来说点别的。
handler是一个处理信息的类。
安卓官方建议将handler生命为静态类，这样的话其内部不会持有外部的引用。
那么就不会导致内存泄露（正常的话，handler会持有外部引用）
然而，这样的话，他又会一直停留在内存之中。
看它的实现。
对于JAVA来说，如果一个类没有构造函数，那么会自动为其生成一个无参构造函数。
这个无参构造函数会自动调用父类的无参构造函数。
如果有了，那么就不会再自动生成。
